# Browser + server API (production default: https://api.devora21.com)
NEXT_PUBLIC_API_BASE_URL=https://api.devora21.com
BACKEND_API_URL=https://api.devora21.com

# Local dev against a machine on your network (optional):
# NEXT_PUBLIC_API_BASE_URL=http://localhost:5000
# BACKEND_API_URL=http://localhost:5000

# Shared secret for Next.js / Netlify → api.devora21.com AI proxy calls (server-only utility).
# Set the same value on the backend server as AI_INTERNAL_API_KEY.
AI_INTERNAL_API_KEY=your_internal_ai_service_key_here

# Browser-direct Claude streaming for resume generation (same value as AI_INTERNAL_API_KEY).
# Required so the client can call https://api.devora21.com/ai/chat/completions/stream.
NEXT_PUBLIC_AI_INTERNAL_API_KEY=your_internal_ai_service_key_here

# DeepSeek API key — set ONLY on api.devora21.com (backend server), not Netlify.
# DEEPSEEK_API_KEY=your_deepseek_api_key_here

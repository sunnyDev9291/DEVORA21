[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Extend serverless timeout for AI resume streaming (requires Netlify Pro for 60s)
[functions]
  node_bundler = "esbuild"


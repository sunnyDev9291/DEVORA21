export const RULE_KEEP_SCORE_MAX = 100;
export const RULE_KEEP_PASS_THRESHOLD = 95;
/** When baseline rule keep is at or above this, ATS edits must not touch bullets/summary. */
export const RULE_KEEP_GUARD_THRESHOLD = 70;
